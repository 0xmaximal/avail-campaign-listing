# Plan: ADHOC-640967E9 — Readme.md icerisinde en sonra Doga SEZER ekle /plan

- Priority: Medium
- Repository: 0xmaximal/avail-campaign-listing
- Suggested test command (not run automatically): `npm test`

## Summary

This ticket requires adding the name "Doğa SEZER" to the end of the `Readme.md` file. This is to ensure our public-facing documentation properly credits all relevant contributors to the project.

## Approach

1.  **Branch Creation:**
    *   Check out the latest version of the primary development branch (e.g., `main` or `develop`).
    *   Create a new feature branch with a descriptive name that includes the ticket number, such as `feature/ADHOC-640967E9-add-contributor`.

2.  **File Modification:**
    *   Locate and open the `Readme.md` file at the root of the repository.
    *   Navigate to the end of the file.
    *   **Decision Point:** If a dedicated "Contributors," "Team," or "Acknowledgements" section exists, add the new name there to maintain semantic structure. If no such section exists, proceed with appending to the absolute end of the file.
    *   Append "Doğa SEZER" on a new line. Ensure there is a line break before the new name to avoid breaking the formatting of the preceding line.

3.  **Local Verification:**
    *   Use your IDE's Markdown preview feature or a local Markdown renderer to view the modified `Readme.md`.
    *   Confirm that the name appears correctly and that the document's layout and formatting are undisturbed.

4.  **Commit and Pull Request:**
    *   Commit the change with a message that adheres to our repository's conventions. The message should clearly state the change and reference the ticket number. Example: `docs: Add Doğa SEZER to Readme.md. Fixes ADHOC-640967E9`.
    *   Push the branch to the remote repository.
    *   Open a Pull Request (PR) targeting the primary development branch. In the PR description, link to this ticket and provide a brief summary for the reviewer.

## Risks & edge cases

*   **Merge Conflicts:** Another developer might be editing `Readme.md` concurrently. The approach mitigates this by requiring a pull from the latest `main`/`develop` before branching, but conflicts must still be resolved carefully if they arise during the PR process.
*   **Formatting Issues:** Simply appending the text without a new line could merge it with the previous line, potentially breaking links, code blocks, or lists. The plan explicitly calls for adding the name on a new line and performing a visual verification step to prevent this.
*   **Character Encoding:** The name "Doğa" contains a non-ASCII character (`ğ`). The file must be saved with UTF-8 encoding to ensure it renders correctly across all operating systems and browsers. Most modern editors handle this by default, but it is a critical consideration.

## Test plan

Automated tests (unit/integration) are not applicable for a change to a static documentation file. Verification will be manual.

*   **Developer Manual Verification:**
    1.  Before committing, the developer assigned to the ticket must use a Markdown preview tool to visually confirm that the name "Doğa SEZER" is present and correctly formatted at the end of the document (or in the appropriate section).
    2.  The developer must also check for any rendering issues related to the special character (`ğ`).

*   **Reviewer Manual Verification (in PR):**
    1.  The PR reviewer must examine the "Files Changed" diff to confirm that only the intended line was added.
    2.  The reviewer should also perform a visual check of the rendered `Readme.md` within the PR UI to ensure no layout or formatting regressions were introduced.

*   **Post-Merge Verification:**
    1.  After the PR is merged, a final check should be performed by viewing the `Readme.md` file on the `main` branch directly in the GitHub UI to confirm it renders as expected for the public.
